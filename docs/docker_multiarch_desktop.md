# Docker Desktop Multiarch

This is already enabled by default. No action needed. Simply switch your arch in aerolab with:

```bash
aerolab config backend -t docker -a arm64
...
aerolab config backend -t docker -a amd64
...
```
