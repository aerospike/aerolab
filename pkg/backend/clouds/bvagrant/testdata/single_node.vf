# This file is generated by aerolab. Do not edit; it is regenerated
# whenever the cluster's nodes change.
Vagrant.configure("2") do |config|
  config.vm.synced_folder ".", "/vagrant", disabled: true
  config.vm.boot_timeout = 600

  config.vm.define "proj-mycluster-1" do |node|
    node.vm.box = "bento/ubuntu-22.04"
    node.vm.hostname = "proj-mycluster-1"
    node.vm.base_mac = "0800273BC068"
    node.vm.network "private_network", ip: "192.168.56.2"

    node.vm.provider "virtualbox" do |vb|
      vb.memory = 2048
      vb.cpus = 2
      vb.name = "proj-mycluster-1"
    end

    node.vm.provider "libvirt" do |lv|
      lv.memory = 2048
      lv.cpus = 2
    end

    node.vm.provider "vmware_desktop" do |v|
      v.vmx["memsize"] = "2048"
      v.vmx["numvcpus"] = "2"
    end

    node.vm.provider "hyperv" do |hv|
      hv.memory = 2048
      hv.cpus = 2
      hv.vmname = "proj-mycluster-1"
    end

    node.vm.provision "shell", inline: <<-SHELL
      set -e
      mkdir -p /root/.ssh
      chmod 700 /root/.ssh
      touch /root/.ssh/authorized_keys
      grep -qF "ssh-ed25519 AAAATESTKEYONLYFORUNITTESTS test@aerolab" /root/.ssh/authorized_keys || echo "ssh-ed25519 AAAATESTKEYONLYFORUNITTESTS test@aerolab" >> /root/.ssh/authorized_keys
      chmod 600 /root/.ssh/authorized_keys
      if [ -d /etc/ssh/sshd_config.d ]; then
        echo "PermitRootLogin prohibit-password" > /etc/ssh/sshd_config.d/60-aerolab.conf
      else
        sed -i 's/^#\?PermitRootLogin.*/PermitRootLogin prohibit-password/' /etc/ssh/sshd_config
      fi
      systemctl reload sshd || systemctl reload ssh || service ssh reload || true
      VAGRANT_UID=$(id -u vagrant 2>/dev/null || echo "")
      if [ -n "$VAGRANT_UID" ]; then
        VAGRANT_HOME=$(getent passwd vagrant | cut -d: -f6)
        mkdir -p "$VAGRANT_HOME/.ssh"
        chmod 700 "$VAGRANT_HOME/.ssh"
        touch "$VAGRANT_HOME/.ssh/authorized_keys"
        grep -qF "ssh-ed25519 AAAATESTKEYONLYFORUNITTESTS test@aerolab" "$VAGRANT_HOME/.ssh/authorized_keys" || echo "ssh-ed25519 AAAATESTKEYONLYFORUNITTESTS test@aerolab" >> "$VAGRANT_HOME/.ssh/authorized_keys"
        chmod 600 "$VAGRANT_HOME/.ssh/authorized_keys"
        chown -R vagrant:vagrant "$VAGRANT_HOME/.ssh" || true
      fi
    SHELL
  end
end
