# [v7.10.1](https://github.com/aerospike/aerolab/releases/tag/7.10.1)

_Release Date: LongMonth DayTh, Year_

**Release Notes:**
* Do not show defaults for otherwise hidden commands.
* Fix WS expiry system to only use Amazon Linux 2023.
* Add safety logic to detaching manually added policies from expiry role in AWS Expiry system.
* Add `--with-eks` to AWS expiry install system, adding the required IAM policies only if that switch is used.
* Add retry logic to GCP availability checks prior to proceeding with cluster creation.
