# [v7.10.0](https://github.com/aerospike/aerolab/releases/tag/7.10.0)

_Release Date: LongMonth DDth, YYYY_

**Release Notes:**
* Add support for aerospike version `8.1` in `conf generate` command.
* Add optional tags to `inventory ansible` command to include AWS tags, GCP labels and Docker labels in the inventory.
* Fix handling of `--type=` in `config backend` command.
